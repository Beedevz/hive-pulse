linters:
  enable:
    - errcheck
    - gosec
    - govet
    - staticcheck
    - unused

linters-settings:
  gosec:
    excludes:
      - G401
      - G501

issues:
  exclude-rules:
    - path: "_test.go"
      linters: [gosec, errcheck]
